// Package providers defines the Provider interface and JDKRelease types.
// The registry (provider lookup) lives in internal/registry to avoid an
// import cycle with the provider implementations.
package providers
